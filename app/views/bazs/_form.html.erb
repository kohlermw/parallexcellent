<%= form_for(@baz) do |f| %>
  <% if @baz.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@baz.errors.count, "error") %> prohibited this baz from being saved:</h2>

      <ul>
      <% @baz.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>

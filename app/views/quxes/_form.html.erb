<%= form_for(@qux) do |f| %>
  <% if @qux.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@qux.errors.count, "error") %> prohibited this qux from being saved:</h2>

      <ul>
      <% @qux.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>

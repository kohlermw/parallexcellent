<%= form_for(@corge) do |f| %>
  <% if @corge.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@corge.errors.count, "error") %> prohibited this corge from being saved:</h2>

      <ul>
      <% @corge.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
